/**
 * @fidscript/types — Shared TypeScript types for FIDScript platform.
 *
 * Single source of truth for all SDK, MCP, and API contracts.
 * Import types from here instead of duplicating them across packages.
 */

// ── Email types ───────────────────────────────────────────────────────────────
export * from './email/index.js';

// ── Event types ──────────────────────────────────────────────────────────────
export * from './events/index.js';
