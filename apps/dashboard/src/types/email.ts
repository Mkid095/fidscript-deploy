// Email types — placeholder for future email domain/mailbox types
// No email types defined yet; stub to avoid empty import failures
export type EmailDomain = Record<string, never>;
export type EmailMailbox = Record<string, never>;
