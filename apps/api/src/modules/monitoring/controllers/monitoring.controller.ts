// Monitoring module entry point — delegates to sub-controllers
// All routes are mounted on /projects/:projectId/monitoring
// See: metrics.controller, alerts.controller, notification-channels.controller
