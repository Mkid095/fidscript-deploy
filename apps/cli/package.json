{
  "name": "@fidscript-deploy/cli",
  "version": "1.1.1",
  "description": "FIDScript Deploy CLI — manage projects, deployments, functions, and more from the terminal",
  "type": "module",
  "bin": {
    "fidscript": "./dist/bin/fidscript.js"
  },
  "scripts": {
    "build": "tsc && node scripts/fix-extensions.mjs",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fidscript-deploy/sdk": "^1.0.2",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
